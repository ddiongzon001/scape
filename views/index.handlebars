<div id="wrapper">
    <h1>Note Scrapper</h1>
    <h2><i>If you can do this...the homework should be no problem.</i></h2>
    <div id="articles">
        {{#each article}}
            <p>{{title}}</p>
            <p>{{summary}}</p>
            <p>{{link}}</p>
        {{/each}}
    </div>
    <div id="notes"></div>
</div>